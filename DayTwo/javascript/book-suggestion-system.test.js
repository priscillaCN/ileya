const {displayMenu} = require('./book-suggestion-system-function')

test("test that display menu functions will return menu when called", () => {

    book-suggestion-system-function.displayMenu()

})
